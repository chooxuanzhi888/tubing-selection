[project]
name = "tubing-selection"
version = "0.1.0"
description = "Upper-completion tubing selection and screening engine."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "numpy>=2.4.4",
    "pandas>=3.0.3",
    "plotly>=6.9.0",
    "streamlit>=1.61.1",
]

[dependency-groups]
dev = [
    "pytest>=9.1.1",
]

[tool.uv]
package = false
